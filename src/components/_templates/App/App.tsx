import { SiteContainer } from "@atoms/SiteContainer";
import { HomePage } from "@components/_templates/HomePage";

export const App = () => {
  return (
    <SiteContainer>
      <HomePage />
    </SiteContainer>
  );
};
